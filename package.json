{
  "name": "string-to-html",
  "version": "1.2.2",
  "description": "A very simple browser script to parse html strings into document fragments",
  "main": "string-to-html.js",
  "scripts": {
    "build": "./node_modules/.bin/browserify test.js -o test-browserify.js -t [ babelify --presets [ es2015 ] ]",
    "test": "npm run build && ./node_modules/.bin/mocha-phantomjs test.html"
  },
  "author": "Rick Lancee",
  "license": "MIT",
  "devDependencies": {
    "babel-preset-es2015": "^6.18.0",
    "babelify": "^7.3.0",
    "browserify": "^13.3.0",
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "mocha-phantomjs": "^4.1.0"
  }
}
